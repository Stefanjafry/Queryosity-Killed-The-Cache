# Bayesian Optimization dependencies for feat/bayesian-opt (Mode B).
#
# Install order matters:
#   1) torch MUST come CPU-only from the PyTorch index, BEFORE botorch,
#      or pip pulls the multi-GB CUDA wheel from PyPI:
#        pip install --no-cache-dir torch --index-url https://download.pytorch.org/whl/cpu
#   2) then: pip install -r requirements-bo.txt
#
# scikit-learn is pinned <1.8 because smac 2.4.0's random-forest
# surrogate imports sklearn.tree._tree.DTYPE, a private symbol removed
# in scikit-learn 1.8+ (undeclared upper bound in smac's metadata).
# Do not upgrade scikit-learn on this branch.
smac==2.4.0
scikit-learn>=1.6.1,<1.8
ConfigSpace>=1.0.0
botorch==0.18.1
gpytorch==1.15.2
linear-operator==0.6.1
